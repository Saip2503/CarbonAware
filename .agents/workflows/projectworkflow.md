---
description: This document outlines the step-by-step dev process for building the Carbon Footprint Awareness platform. It integrates cross-platform mobile-first design principles, a serverless Firebase arch, and  design validation using Stitch mcp
---

## Phase 1: Environment & Repository Setup
* **Repository Creation:** Initialize a public GitHub repository. All development must occur strictly within the `main` branch.
* **AI Platform Cloning:** Clone the repository directly into your AI development platform workspace.
* **Project Initialization:** Scaffolding the core platform using Flutter for web and mobile configuration:
  ```bash
  flutter create . --platforms web

Asset Optimization: Ensure no heavy media files or local assets exceed the strict 10 MB repository threshold. Use icon packs and network-hosted resources.Phase 2: Design Validation (Stitch AI MCP Integration)Before writing frontend code, validate the UI/UX components using the Stitch AI MCP server to guarantee a highly responsive, polished, mobile-first design hierarchy.Design Ingestion: Use the Stitch AI MCP tool to check the visual wireframes and layout structures against your design prompt parameters.Artifact Review: Inspect the generated layout layout grids, color contrast adherence (Forest Green #2E7D32 on Light Beige background), and touch target dimensions.Iterative Refinement: Modify component specs directly through the MCP interface until the design system is locked in, minimizing UI refactoring during coding.Phase 3: Database & Authentication ArchitectureFirebase Connection: Wire up firebase_core, firebase_auth, and cloud_firestore dependencies inside pubspec.yaml.User Authentication: Build an email/password registration and login system that instantiates a user profile inside the Firestore users collection.NoSQL Document Modeling: Implement structured data mapping for tracking metrics:users/{userId}: Account details, goals, and targets.users/{userId}/daily_logs/{YYYY-MM-DD}: Quantifiable metrics for transit miles, vehicle variants, dietary types, and energy units.Security Layer: Author and deploy Firestore Security Rules to enforce strict document isolation so that authenticated users can only access their matching UID records.Phase 4: Dynamic Assistant & Smart Logic EngineContext Aggregator: Build a repository service layer that pulls the past 7 days of daily footprint metrics into a single lightweight JSON payload.LLM Prompt Orchestration: Feed the compiled user history context into the dynamic AI engine (Gemini API / Vertex AI) to generate localized, real-world, actionable environmental tips.Insight Interface: Create a responsive dashboard container widget to dynamically render the AI-generated feedback loops based on the highest emission category.Phase 5: Code Quality & Testing PassCode Quality Refactoring: Break down bloated widgets into modular, single-responsibility presentational components. Ensure declarative code architectures.Mathematical Validation: Write unit tests to confirm the core translation engine converts varying distances, vehicle types, and dietary selections accurately into precise kilograms of CO2 equivalent ($kg\text{ }CO_2e$).Accessibility Inspections: Wrap input objects in Semantics tags and run automated accessibility contrast evaluations to ensure universal usability.Phase 6: Production Verification & SubmissionRepository Health Check: Confirm that the overall bundle size remains securely below 10 MB and that no secondary development branches exist.Documentation Compilation: Verify the comprehensive README.md clearly profiles the chosen sustainability vertical, system logic architecture, configuration instructions, and core mathematical assumptions.Deployment & Push: Execute final commits, push to the public remote origin, and deploy the compilation target.